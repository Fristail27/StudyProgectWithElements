import React, {useMemo, useState} from "react";
import Clock from "./Clock";

export default {
    title: "clock demo",
    component: Clock
}

export const BaseExample = () => {
    return <Clock/>
}